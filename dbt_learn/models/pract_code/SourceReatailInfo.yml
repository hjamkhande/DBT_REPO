sources:
  - name : src_retail_schema
    database : learndbt
    schema : retail
    tables:
      - name: tb_cust
        identifier: customer
      - name : tb_nation
        identifier: nation
      - name : tb_region
        identifier: region
  - name : src_hooks
    database : hooks
    schema : src
    tables: 
       - name : tb_emp_hooks
         identifier : empsrc
